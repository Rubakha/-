meta:
  name: alisa-nevskaya-bot

environment:
  python: "3.11"

build:
  buildType: pip
  buildFile: requirements.txt

run:
  startCommand: python bot_best.py

persistenceMount:
  - mountPath: /data
    size: 1Gi

env:
  PYTHONUNBUFFERED: "1"
  DATA_DIR: /data
